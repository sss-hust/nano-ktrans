---
date: 2026-04-07
tags: [journal]
---

# 开发日志 2026-04-07

## 完成的工作

- 初始化项目知识库 (`.knowledge/` 目录)
- 将项目依赖拆分为 core / `cuda` / `cpu-kernel` / `accel`
- 补齐无 `flash-attn`、无 `kt-kernel`、无 CUDA 时的纯 PyTorch fallback
- 新增 CPU-only smoke test，验证 prefill + decode 主链路
- 用本机 `/home/yangfu/models/Qwen--Qwen3-30B-A3B-Base` 实际跑通 `example.py --device cpu --max-new-tokens 1`

## 关键发现

- 当前机器没有可用 NVIDIA 驱动，`nvidia-smi` 不可用
- `pip install` 需要绕开失效的本地代理环境变量
- `chunked prefill` 返回的是最后一个 chunk 的 logits，而不是整段 prompt 的 logits
- Qwen3-MoE 的 `head_dim` 必须从配置显式读取，不能从 `hidden_size / num_heads` 推导

## 遗留问题

- 需要用真实 MoE checkpoint 验证 `LLM` 入口
- 需要继续补充架构说明 (`architecture/overview.md`)
- PIM 路径还未接入

## 决策记录

- 采用嵌套式知识库结构管理项目知识
- 在默认安装下优先保证 CPU-only 可运行，再把 CUDA / `kt-kernel` / PIM 作为可选增强
